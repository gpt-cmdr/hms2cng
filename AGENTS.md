# AGENTS.md

Read `CLAUDE.md` for the repository's development and architecture guidance.

## Documentation Site

- The canonical documentation is `https://rascommander.info/hms2cng/`.
- The `ras-commander-docs` umbrella builds this repo's `master` branch and injects the shared
  cross-product theme at publish time. Do not copy the umbrella theme into this repo.
- `hms2cng.readthedocs.io` is a legacy URL and should redirect to the canonical site.
- Keep `hms2cng/schemas.py` synchronized with public DataFrame/GeoDataFrame contract changes.
